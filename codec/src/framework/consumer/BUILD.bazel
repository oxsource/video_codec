package(default_visibility = ["//src/framework:__subpackages__", "//tests:__subpackages__", "//src/examples:__subpackages__"])

# PacketConsumer interface, PacketPump drain loop, file sinks (FileSinkConsumer,
# FileByteSink), and the PacketConsumer adapters over format converters
# (Mp4FileConsumer: FileByteSink + mux::Mp4Muxer).
# Depends on core (types), queue (PacketSource), api, and mux (format).
cc_library(
    name = "consumer",
    srcs = glob(["*.cc"]),
    hdrs = glob(["*.h"]),
    includes = [".."],
    deps = [
        "//src/framework/core",
        "//src/framework/api",
        "//src/framework/queue",
        "//src/framework/mux",
    ],
)
