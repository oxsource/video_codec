package(default_visibility = ["//src/framework:__subpackages__", "//tests:__subpackages__"])

# PacketConsumer interface, PacketPump drain loop, FileSinkConsumer.
# Depends on core (types), queue (EncodedPacketSource), api (per module graph).
cc_library(
    name = "consumer",
    srcs = glob(["*.cc"]),
    hdrs = glob(["*.h"]),
    includes = [".."],
    deps = [
        "//src/framework/core",
        "//src/framework/api",
        "//src/framework/queue",
    ],
)
